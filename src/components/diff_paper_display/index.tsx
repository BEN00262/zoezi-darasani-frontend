// this just gets the data requested :) ---> we will need to watch out for the index of the paper :)
// get the questions and then we render it :)
const DiffPaperDisplay = () => {
    // request the diff for a given paper :)
    
    return (
        <>
            {/* we actually use the same data set :) */}
        </>
    )
}

export default DiffPaperDisplay